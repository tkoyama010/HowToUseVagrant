sudo vagrant ssh
vagrant
sudo apt-get install vagrant
sudo vagrant box add quantal64 https://github.com/downloads/roderik/VagrantQuantal64Box/quantal64.box
sudo vagrant init quantal64
sudo vagrant up
sudo vagrant ssh

